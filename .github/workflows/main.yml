name: Notion Backup
on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * *'

jobs:
  backup:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v3
      
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          
      # Bước này xóa thư viện cũ và cài bản mới nhất (Fix lỗi Database)
      - name: Install Tools (Force Update)
        run: |
          rm -rf node_modules package-lock.json
          npm install @notionhq/client@latest notion-to-md@latest axios slugify
          
      - name: Run Backup
        env:
          # Bot cần 2 dòng này để biết ID và Token là gì
          NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}
          # Dòng dưới sẽ thử lấy cả 2 tên secret phổ biến để tránh lỗi
          NOTION_PAGE_ID: ${{ secrets.NOTION_PAGE_ID || secrets.NOTION_SPACE_ID }}
        run: node backup.js
          
      - name: Commit and Push
        run: |
          git config user.name "Backup Bot"
          git config user.email "bot@noreply.github.com"
          git add .
          git commit -m "Daily Notion Backup" || echo "No changes to commit"
          git push
