name: Notion Backup
on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * *'

jobs:
  backup:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v3
      
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          
      - name: Run Backup in Clean Room
        env:
          NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}
          NOTION_PAGE_ID: ${{ secrets.NOTION_PAGE_ID || secrets.NOTION_SPACE_ID }}
        run: |
          # 1. Tạo phòng cách ly (thư mục mới)
          mkdir clean_room
          
          # 2. Copy code vào đó
          cp backup.js clean_room/
          
          # 3. Vào phòng và cài đặt thư viện MỚI NHẤT (Bỏ qua thư mục node_modules cũ ở ngoài)
          cd clean_room
          npm init -y
          npm install @notionhq/client@latest notion-to-md@latest axios slugify
          
          # 4. Chạy code
          node backup.js
          
          # 5. Đem kết quả (file .md và ảnh) ra ngoài để lưu
          cp *.md ../ || true
          if [ -d "images" ]; then cp -r images ../; fi

      - name: Commit and Push
        run: |
          git config user.name "Backup Bot"
          git config user.email "bot@noreply.github.com"
          git add .
          git commit -m "Daily Notion Backup" || echo "No changes to commit"
          git push
